plugins {
    id 'base'
    id 'net.fabricmc.fabric-loom' version "${loom_version}" apply false
    id 'net.neoforged.moddev' version "${moddev_version}" apply false
}
tasks.named('build') { dependsOn ':fabric:build', ':neoforge:build' }
tasks.named('assemble') { dependsOn ':fabric:assemble', ':neoforge:assemble' }
tasks.named('check') { dependsOn ':fabric:check', ':neoforge:check' }
tasks.register('test') {
    group = 'verification'
    dependsOn ':fabric:test', ':neoforge:test'
}
